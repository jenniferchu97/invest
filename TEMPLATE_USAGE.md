# 杠杆率计算模板使用说明

## 文件说明

`leverage_calculation_template.xlsx` 是一个完整的 Excel 杠杆率计算模板文件，用于三个债券账户的实时杠杆率计算。

## 账户信息

模板包含三个 Sheet，分别对应三个账户：

| 账户名称 | Sheet 名称 | 杠杆限制 |
|---------|-----------|---------|
| 银利3号 | 银利3号 | 140% (1.40) |
| 银利5号 | 银利5号 | 140% (1.40) |
| 健康险 | 健康险 | 180% (1.80) |

## Excel 结构

每个 Sheet 包含四个部分：

### 第一部分：初始资产负债表数据区

需要输入以下数据（黄色背景的单元格）：

- **数据日期**：输入数据的日期
- **总资产**：账户的总资产金额
- **总负债**：账户的总负债金额
- **卖出回购金融资产款**：卖出回购的金额
- **交易性金融负债-买断**：买断式交易金融负债
- **交易性金融负债-借券**：借券式交易金融负债
- **质押式逆回购**：质押式逆回购金额

自动计算项（绿色背景）：
- **净资产** = 总资产 - 总负债
- **基准杠杆率** = 总资产 / 净资产

### 第二部分：当日操作输入区

输入当日的操作数据（黄色背景的单元格）：

| 操作类型 | 说明 |
|---------|------|
| 买卖券净额 | 正数为净买入，负数为净卖出 |
| 借券卖空 | 增加借券杠杆 |
| 买断逆回购 | 增加回购和借券杠杆 |
| 增减资 | 正数增资，负数减资 |
| 其他调整 | 其他需要调整的金额 |

### 第三部分：变化前后对比区

系统自动计算以下指标的变化前、变化后和变化量：

1. **总资产**
2. **净资产**
3. **杠杆率** = 总资产 / 净资产
4. **回购规模**
5. **回购杠杆** = 卖出回购金融资产款 / 净资产
6. **借券规模**
7. **借券杠杆** = (交易性金融负债-买断 + 交易性金融负债-借券) / 净资产
8. **质押式逆回购规模**
9. **质押式逆回购杠杆** = 质押式逆回购 / 净资产

### 第四部分：杠杆限制预警区

显示：
- **当前杠杆率**：变化后的杠杆率
- **杠杆限制**：该账户的杠杆限制值
- **预警状态**：
  - 如果当前杠杆率 ≤ 杠杆限制：显示"正常"
  - 如果当前杠杆率 > 杠杆限制：显示"超过杠杆限制！"（红色背景）

## 使用步骤

1. **打开文件**：打开 `leverage_calculation_template.xlsx`

2. **选择账户**：点击底部的 Sheet 标签选择要操作的账户（银利3号、银利5号或健康险）

3. **填写初始数据**：
   - 在第一部分中填写数据日期
   - 填写总资产、总负债
   - 填写详细项目（卖出回购金融资产款、交易性金融负债等）
   - 系统会自动计算净资产和基准杠杆率

4. **输入当日操作**：
   - 在第二部分中填写当日的各项操作金额
   - 注意正负号的含义（如买入为正，卖出为负）

5. **查看结果**：
   - 第三部分会自动显示所有指标的变化前后对比
   - 第四部分会显示当前杠杆率和预警状态

6. **检查预警**：
   - 如果预警状态显示红色的"超过杠杆限制！"，需要调整操作以降低杠杆率

## 格式说明

- **黄色背景**：需要手动输入的数据单元格
- **绿色背景**：由公式自动计算的单元格
- **蓝色背景**：标题和表头
- **红色背景**：超过杠杆限制的警告
- **数字格式**：所有金额和比率均显示为两位小数
- **冻结窗格**：已设置冻结窗格，便于滚动时查看标题

## 公式说明

### 基本公式

1. **杠杆率** = 总资产 / 净资产
2. **回购杠杆** = 卖出回购金融资产款 / 净资产
3. **借券杠杆** = (交易性金融负债-买断 + 交易性金融负债-借券) / 净资产
4. **质押式逆回购杠杆** = 质押式逆回购 / 净资产

### 变化后计算

1. **变化后总资产** = 初始总资产 + 买卖券净额 + 借券卖空 + 买断逆回购
2. **变化后净资产** = 初始净资产 + 增减资 + 其他调整
3. **变化后回购规模** = 初始卖出回购金融资产款 + 买断逆回购
4. **变化后借券规模** = 初始借券规模 + 借券卖空 + 买断逆回购

## 生成新模板

如果需要重新生成模板文件，可以运行以下命令：

```bash
python3 generate_leverage_template.py
```

## 验证模板

验证模板文件的完整性和正确性：

```bash
python3 verify_template.py
```

## 注意事项

1. 所有金额单位需要保持一致
2. 正负号的使用要准确（买入/增资为正，卖出/减资为负）
3. 定期检查杠杆率，确保不超过限制
4. 建议每日更新数据，及时发现风险
5. 如有疑问，请参考本说明文档或联系系统管理员

## 示例

### 示例 1：正常操作

假设银利3号账户：
- 初始总资产：1,000,000
- 初始总负债：600,000
- 初始净资产：400,000
- 基准杠杆率：2.50

当日操作：
- 买卖券净额：+50,000（买入）
- 增减资：0

变化后：
- 总资产：1,050,000
- 净资产：400,000
- 杠杆率：2.625
- 预警状态：超过杠杆限制！（因为 2.625 > 1.40）

### 示例 2：增资降杠杆

为降低杠杆率，进行增资：
- 增减资：+250,000

变化后：
- 净资产：650,000
- 杠杆率：1.615→1.38
- 预警状态：正常

## 技术支持

如有问题，请联系系统开发团队。

---

**版本**: 1.0  
**创建日期**: 2026-01-31  
**最后更新**: 2026-01-31
